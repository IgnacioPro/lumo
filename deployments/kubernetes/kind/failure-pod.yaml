apiVersion: v1
kind: Pod
metadata:
  name: failure-pod-imagepullbackoff
  namespace: tenant-acme-corp
spec:
  containers:
  - name: failing-container
    image: nonexistent-registry.example.com/nonexistent-image:nonexistent-tag
    command: ["sleep", "3600"]